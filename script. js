function chat() {
    alert("🚀 AI Chat is coming soon!");
}

function image() {
    let prompt = document.getElementById("prompt").value;

    if(prompt==""){
        alert("Please enter a prompt.");
        return;
    }

    alert("Generating AI Image for:\n\n" + prompt);
}

function video() {
    let prompt = document.getElementById("prompt").value;

    if(prompt==""){
        alert("Please enter a prompt.");
        return;
    }

    alert("Generating AI Video for:\n\n" + prompt);
}

function photo() {
    alert("Photo Editor coming soon.");
}

function editvideo() {
    alert("Video Editor coming soon.");
}